<div class="navbar">
  <!-- Logo -->
  <%= link_to image_tag("newgamepluslogo.png", alt: "logo"), '/', class: "navbar-wagon-brand" %>

  <!-- Right Navigation -->
  <div class="navbar-wagon-right hidden-xs hidden-sm">

    <!-- Search form -->
    <!-- <form action="" class="navbar-wagon-search">
      <input type="text" class="navbar-wagon-search-input" placeholder="Search something">
      <button type="submit" class="navbar-wagon-search-btn">
        <i class="fa fa-search"></i>
      </button>
    </form> -->


    <!-- Notification link-->
<!--     <a href="" class="navbar-wagon-item navbar-wagon-link">
      <div class="badge-container">
        <i class="fa fa-envelope-o"></i>
        <div class="badge badge-blue">3</div>
      </div>
    </a>
 -->
    <!-- Profile picture and dropdown -->
    <div class="navbar-wagon-item">
      <div class="dropdown">
        <% if user_session.nil? %>
          <%= link_to "Log in", new_user_session_path, class: "btn btn-orange" %>
        <% else %>
          <% if current_user[:facebook_picture_url].nil? %>
            <%= image_tag("defaultuserimage.png", alt: "you", class: "avatar dropdown-toggle", id: "navbar-wagon-menu", data: {toggle: "dropdown"}) %>
          <% else %>
            <img src=<%= current_user[:facebook_picture_url] %> class="avatar dropdown-toggle" id="navbar-wagon-menu" data-toggle="dropdown">
          <% end %>
          <ul class="dropdown-menu dropdown-menu-right navbar-wagon-dropdown-menu">
            <li><%= link_to "Profile", user_path(current_user) %></li>
            <li><%= link_to "Sell a game", new_user_product_offer_path(current_user) %></li>
            <li><%= link_to "Log out", destroy_user_session_path, method: :delete %></li>
          <% end  %>
        </ul>
      </div>
    </div>

    <!-- Button (call-to-action) -->
    <!-- Button (See Sell Game) -->
      <% if user_session.nil? %>
        <%= link_to "Sign up", new_user_registration_path, class: "navbar-wagon-item btn-yellow btn" %>
      <% else %>
        <%= link_to "Sell a game!", new_user_product_offer_path(current_user), class: "navbar-wagon-item btn-yellow btn" %>
      <% end  %>
  </div>

  <!-- Dropdown appearing on mobile only -->
  <div class="navbar-wagon-item hidden-md hidden-lg">
    <div class="dropdown dropdown-mobile">
        <% if user_session.nil? %>
          <%= link_to "Log in", new_user_session_path, class: "btn btn-orange" %>
        <% else %>
          <i class="fa fa-bars dropdown-toggle" data-toggle="dropdown"></i>
          <ul class="dropdown-menu dropdown-menu-right navbar-wagon-dropdown-menu">
            <li><%= link_to "Profile", user_path(current_user) %></li>
            <li><%= link_to "Sell a game", new_user_product_offer_path(current_user) %></li>
            <li><%= link_to "Log out", destroy_user_session_path, method: :delete %></li>
          </ul>
        <% end %>
    </div>
  </div>
</div>
