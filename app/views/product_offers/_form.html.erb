<%= simple_form_for [current_user, @product_offer] do |po| %>

  <!-- po.input :product_id, collection: Product.all -->
  <%= po.input :gid, value: 0, as: :hidden, id: 'game-id' %>

  <%= po.input :price %>
  <%= po.input :location %>
  <%= po.button :submit %>

<% end %>
